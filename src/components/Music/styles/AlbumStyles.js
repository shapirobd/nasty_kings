import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	albumName: {
		color: "white",
		marginBottom: "20px",
	},
	albumCover: {
		width: "100%",
	},
}));
