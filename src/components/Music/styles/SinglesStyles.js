import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	singlesHeader: {
		color: "white",
	},
	albumsContainer: {
		margin: "30px 0 40px",
	},
}));
