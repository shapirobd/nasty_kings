import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
	singlesHeader: {
		color: "white",
		fontWeight: "bold",
	},
	albumsContainer: {
		margin: "30px 0",
	},
}));
